# Build outputs.
_novo/

# The built package binary — a library package still produces one,
# because its manifest needs a `main`.
schema-nv

# Generated from the sources by `novo doc`; a committed copy is a
# second original.
APIDOC.md

# Resolved deps.  There are none — the JSON value and the regex engine
# are both the standard library's.
novo.lock
